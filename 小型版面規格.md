# 小型版面規格

小型表單版面規則,供 OCR 辨識與前端自動帶入使用。

## 版面結構

每頁為一張表;每番次 = **3 子列(R1~R3)**。欄以垂直格線分割為 **C1~C19**。

- **C1(跨 3R):番次**(第 X 番)
- **C2(跨 3R):品項**
- C3:**忽略**(非序;序由人工填寫)
- **C4:R1+R2 合併辨識 模具編號 6 格(墨跡常落在 R1/R2 交界,故不把 R1 當獨立空白列)/ R3 離心開始、離心結束**
- **C5~C8:R1+R2 合併辨識 加料、慢速、中速、高速轉速(同 C4,墨跡可能在交界)/ R3 加料、慢速、中速、高速時間**
- **C9(3R):蒸養池編號 + 入池時間**(特殊組合規則,見下)
- C10~C16:**忽略**(空白或人工備註)
- **C17(3R):蒸養溫度1~3**(R1=1、R2=2、R3=3)
- **C18(3R):蒸養階段1~3**(R1=1、R2=2、R3=3)
- **C19:發現情形**(人工備註,忽略 OCR)

## OCR 規則

### 模具編號(C4 R1+R2)
- 6 格模具編號 = CSV 轉位1~6,照出現順序。
- R1 並非穩定空白:墨跡常落在 R1/R2 交界 → **R1+R2 合併裁切為單一辨識區域**,一次讀出 6 格(避免把 R2 字跡切掉)。

### 轉速 / 轉速時間(C5~C8)
- R1+R2 合併辨識 = 轉速(R1 同 C4 可能沾墨,合併裁切);R3 = 時間。

### 蒸養池 + 入池時間(C9)
- 偵測墨跡;僅 3 列直式數字:
  - 只有 1 個數字 → 蒸養池編號
  - 3 個數字直列(如 `1 / 14 / 03`)→ 第 1 個 = 蒸養池編號,後兩個合併 = 入池時間 `1403`

### 蒸養溫度 / 蒸養階段(C17 / C18)
- 每列讀一個數字:R1=欄1、R2=欄2、R3=欄3。

## 衍生規則(前端自動帶入)

| 欄位 | 規則 |
|------|------|
| 序 | **人工填寫**(非 OCR、非自動帶入) |
| 排列1~6(小型) | = 轉位1~6 直接複製(模具編號原順序,不排序) |
| 位置 | 同蒸養池分組、按番次順序:前番=下、後番=中(先入後出) |
| 入池時間 | 同池前番 ← 後番(後番 C9 才有寫,兩番共用同一入池時間) |
| 生產數量(支數) | = 品項對照(支/模具) × 模具個數 |

## 中型排列對照(僅供比對)

- 3 模具:排列1~4 = [a, b, 空白, c](例 轉位=[1,4,3] → 排列=[1,4,'',3])
- 4 模具:排列1~4 = 模具編號照順序(= 轉位複製)
- 中型以 C13 圈選數字為主要來源,與轉位做衝突檢查。
